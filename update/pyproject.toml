[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "frida-nix-update"
version = "0.0.0"
dependencies = ["packaging"]

[project.scripts]
frida-nix-update = "frida_nix_update:main"
